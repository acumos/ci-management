---
- project:
    name: on-boarding-project
    project-name: on-boarding-app
    project: on-boarding
    mvn-params: -f onboarding-app/pom.xml
    mvn-settings: on-boarding-settings
    jobs:
      - '{project-name}-maven-jobs'
      # merge job is not yet part of global JJB
      - gerrit-maven-merge
    stream:
      - master:
          branch: master
