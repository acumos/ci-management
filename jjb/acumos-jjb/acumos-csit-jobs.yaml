---
- job-template:
    name: '{project-name}-{stream}-verify-csit-{functionality}'
    disabled: false

    project-type: freestyle
    build-node: centos7-docker-4c-4g
    node: '{build-node}'

    properties:
      - lf-infra-properties:
          build-days-to-keep: '{build-days-to-keep}'

    parameters:
      - infra-parameters:
          branch: '{branch}'
          refspec: 'refs/heads/{branch}'
          artifacts: '{archive-artifacts}'
      - test-automation-test-plan:
          test-plan: 'plans/{project-name}/{functionality}'
      - test-automation-test-options:
          test-options: '{robot-options}'

    scm:
      - gerrit-trigger-scm:
          refspec: '$GERRIT_REFSPEC'
          choosing-strategy: 'gerrit'
          submodule-recursive: '{submodule-recursive}'

    wrappers:
      - lf-infra-wrappers:
          build-timeout: '{build-timeout}'
          jenkins-ssh-credential: '{jenkins-ssh-credential}'

    triggers:
      - gerrit-trigger-patch-submitted:
          server: '{server-name}'
          branch: '{branch}'
          files: 'test/csit/**/{project-name}/**'

    builders:
      - test-automation-install-robotframework
      - inject:
          properties-file: 'env.properties'
      - test-automation-run-test

    publishers:
      - test-automation-robot:
          unstable-if: 0.0
          pass-if: 100.0
      - lf-infra-publish

- job-template:
    name: '{project-name}-{stream}-csit-{functionality}'
    disabled: false

    project-type: freestyle
    build-node: centos7-docker-4c-4g
    node: '{build-node}'

    properties:
      - lf-infra-properties:
          build-days-to-keep: '{build-days-to-keep}'

    parameters:
      - infra-parameters:
          branch: '{branch}'
          refspec: 'refs/heads/{branch}'
          artifacts: '{archive-artifacts}'
      - test-automation-test-plan:
          test-plan: 'plans/{project-name}/{functionality}'
      - test-automation-test-options:
          test-options: '{robot-options}'

    scm:
      - gerrit-trigger-scm:
          refspec: ''
          choosing-strategy: 'default'
          submodule-recursive: '{submodule-recursive}'

    wrappers:
      - lf-infra-wrappers:
          build-timeout: '{build-timeout}'
          jenkins-ssh-credential: '{jenkins-ssh-credential}'

    triggers:
      - gerrit-trigger-patch-merged:
          server: '{server-name}'
          branch: '{branch}'
          files: 'test/csit/**/{project-name}/**'
      - timed: '@daily'
      - reverse:
          jobs: '{obj:trigger_jobs}'

    builders:
      - test-automation-install-robotframework
      - inject:
          properties-file: 'env.properties'
      - test-automation-run-test

    publishers:
      - test-automation-robot:
          unstable-if: 0.0
          pass-if: 100.0
      - lf-infra-publish
