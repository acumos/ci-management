---
# Global macros

# PARAMETERS
- parameter:
    name: pypi-server
    parameters:
    - string:
        name: PYPI_SERVER
        default: '{pypi_server}'
        description: 'PyPI repository name'
